function battleHandling(player: int[], weaponDMG: int, monster: int[]): int[] {
    //roll
    const playerRoll = Math.floor(Math.random() * 19) + 1;
    conts playerDMG = player[0] + weaponDMG;
    const monsterDMG = monster[0];
    const hitChance = monster[1]

    if (playerRoll >= hitChance) {
        monster[0] -= playerDMG;
    } else {
        //miss
    }
}
