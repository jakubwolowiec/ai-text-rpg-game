# Example environment variables for the frontend
# Set this to 'true' to enable Claude Haiku 4.5 for all clients.
REACT_APP_ENABLE_CLAUDE_HAIKU=true

# Note: The frontend will send a `model` field in API requests when this flag
# is enabled. Ensure the backend supports and honors a `model` parameter in
# the `/initialize` and `/action` endpoints (e.g., selecting Claude Haiku 4.5).
# API base URL (optional; configure as needed):
# REACT_APP_API_BASE_URL=https://your-backend.example.com

# Database Configuration for Oracle HeatWave (MySQL)
DB_HOST=your-heatwave-host.mysql.database.azure.com
DB_USER=your-username
DB_PASSWORD=your-password
DB_NAME=your-database-name
DB_PORT=3306

# Server Port
PORT=5000
